---
phase: 04-core-rename
plan: 04
type: execute
wave: 1
depends_on: ["04-03"]
files_modified: ["src/engine.rs"]
autonomous: true
requirements: [REN-04]
gap_closure: true

must_haves:
  truths:
    - "cargo build succeeds with zero warnings"
    - "RuleLifetime has no Turns or Triggers variants"
    - "No dead pattern-match arms reference Turns or Triggers in dispatch"
  artifacts:
    - path: "src/engine.rs"
      provides: "Engine runtime — RuleLifetime enum with only Permanent variant"
      contains: "enum RuleLifetime"
  key_links:
    - from: "src/engine.rs"
      to: "RuleLifetime::Permanent"
      via: "add_behavior always inserts Permanent; all match arms simplified"
      pattern: "RuleLifetime::Permanent"
---

<objective>
Remove the never-constructed `Turns(u32)` and `Triggers(u32)` variants from the private `RuleLifetime` enum in `src/engine.rs`, and delete the dead pattern-match arms that reference them.

Purpose: `cargo build` emits one `dead_code` warning because `add_behavior` always inserts `RuleLifetime::Permanent` — the `Turns` and `Triggers` constructors are never called. The Phase 4 success criterion requires zero warnings. Removing the dead variants is cleaner than suppressing the warning.

Output: `src/engine.rs` with a single-variant `RuleLifetime` enum; `cargo build` emits zero warnings.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead RuleLifetime variants and their pattern-match arms</name>
  <files>src/engine.rs</files>
  <action>
    In `src/engine.rs`, make the following targeted changes:

    1. **Simplify the `RuleLifetime` enum (lines 14-19).** Remove the `Turns(u32)` and `Triggers(u32)` variants. The enum should become:
       ```rust
       #[derive(Clone, Copy, Debug)]
       enum RuleLifetime {
           Permanent,
       }
       ```

    2. **Remove the `Triggers` decrement block in `dispatch` (around lines 341-348).** Delete the entire `if let Some(RuleLifetime::Triggers(n)) = ...` block inside the first behavior loop. That branch is now unreachable because no `Triggers` variant exists.

    3. **Remove the `Turns` decrement block in `dispatch` (around lines 364-373).** Delete the entire `for (id, lifetime) in self.lifetimes.iter_mut()` loop that contains the `if let RuleLifetime::Turns(n) = lifetime` arm. That loop exists solely to decrement turn counts; with only `Permanent` remaining there is nothing to decrement.

    4. **Update the docstring comment above `dispatch` (around lines 301-302)** that mentions `Turns` and `Triggers` by name. Remove the sentence referencing them, or replace with: "Behavior lifetimes are managed per-dispatch (all behaviors registered with `add_behavior` are permanently enabled until explicitly removed)."

    Do NOT touch any other logic. The `lifetimes` field and `lifetime_snapshot` in `CommitFrame` can remain as `HashMap<&'static str, RuleLifetime>` — they will always contain only `Permanent` entries, which is harmless. Phase 5 will remove the `lifetimes` map entirely when it introduces `is_active()`.

    Verify there are zero remaining references to `Turns` or `Triggers` after the edit with:
    ```
    grep -n "Turns\|Triggers" src/engine.rs
    ```
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -E "^warning|^error" | head -20; cargo build 2>&1 | grep -c "^warning" | xargs -I{} sh -c 'test {} -eq 0 && echo "PASS: zero warnings" || echo "FAIL: {} warnings"'</automated>
  </verify>
  <done>
    `cargo build` completes with zero errors and zero warnings. `grep "Turns\|Triggers" src/engine.rs` returns no output. The `RuleLifetime` enum contains only the `Permanent` variant.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm full test suite still passes</name>
  <files></files>
  <action>
    Run the full test suite and both examples to confirm no behavioral regression from removing the dead variants.

    ```
    cargo test
    cargo run --example tictactoe
    cargo run --example backgammon
    ```

    All 17 tests must pass. Both examples must produce the same output they produced before this change (the dead variants were never exercised by any test or example).
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5</automated>
  </verify>
  <done>
    `cargo test` reports all tests passed with zero failures. `cargo run --example tictactoe` and `cargo run --example backgammon` exit with status 0 and produce correct output.
  </done>
</task>

</tasks>

<verification>
- `cargo build` emits zero warnings (grep for "^warning" returns no matches)
- `cargo test` passes (all 17 tests green)
- `cargo run --example tictactoe` exits 0
- `cargo run --example backgammon` exits 0
- `grep "Turns\|Triggers" src/engine.rs` returns no output
- `grep "enum RuleLifetime" src/engine.rs` returns exactly 1 line containing only `Permanent`
</verification>

<success_criteria>
`cargo build` succeeds with zero warnings. The `RuleLifetime` enum in `src/engine.rs` has only the `Permanent` variant. All 17 tests pass. Both examples run correctly. Phase 4 success criterion #1 ("cargo build succeeds with no warnings") is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-rename/04-04-SUMMARY.md`
</output>
