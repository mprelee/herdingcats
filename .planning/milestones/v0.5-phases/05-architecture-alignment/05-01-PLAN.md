---
phase: 05-architecture-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/outcome.rs
  - src/behavior.rs
  - src/engine.rs
  - src/lib.rs
  - examples/tictactoe.rs
  - examples/backgammon.rs
autonomous: true
requirements: [SC-1, SC-5, SC-6]

must_haves:
  truths:
    - "BehaviorResult::Stop wraps NonCommittedOutcome<N> — behaviors explicitly choose InvalidInput, Disallowed, or Aborted"
    - "Dispatch maps Stop(NonCommittedOutcome::X(n)) to Outcome::X(n) via From impl — no hardcoded Aborted coercion"
    - "NonCommittedOutcome is re-exported at crate root"
    - "All existing tests pass with the new Stop contract"
    - "Both examples compile and run with the new API"
  artifacts:
    - path: "src/outcome.rs"
      provides: "NonCommittedOutcome<N> enum + From<NonCommittedOutcome<N>> for Outcome<F, N>"
      contains: "pub enum NonCommittedOutcome"
    - path: "src/behavior.rs"
      provides: "BehaviorResult<D, N> with Stop(NonCommittedOutcome<N>)"
      contains: "Stop(NonCommittedOutcome<N>)"
    - path: "src/lib.rs"
      provides: "pub use crate::outcome::NonCommittedOutcome"
      contains: "NonCommittedOutcome"
  key_links:
    - from: "src/behavior.rs"
      to: "src/outcome.rs"
      via: "use crate::outcome::NonCommittedOutcome"
      pattern: "NonCommittedOutcome"
    - from: "src/engine.rs"
      to: "src/outcome.rs"
      via: "outcome.into() in dispatch Stop arm"
      pattern: "outcome\\.into\\(\\)"
---

<objective>
Fix the BehaviorResult/NonCommittedOutcome/Outcome contract so behaviors explicitly choose InvalidInput, Disallowed, or Aborted instead of all Stops coercing to Aborted.

Purpose: This is the foundation for the entire phase — all subsequent plans depend on the outcome contract being stable.
Output: NonCommittedOutcome<N> enum, updated BehaviorResult<D, N>, updated dispatch arm, updated tests/examples.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-architecture-alignment/05-CONTEXT.md
@.planning/phases/05-architecture-alignment/05-RESEARCH.md

<interfaces>
<!-- Current types that will be modified. Executor uses these as starting point. -->

From src/outcome.rs:
```rust
pub struct Frame<E: EngineSpec> {
    pub input: E::Input,
    pub diffs: Vec<E::Diff>,
    pub traces: Vec<E::Trace>,
    pub reversibility: Reversibility,  // Will be removed in Plan 02, not this plan
}

pub enum Outcome<F, N> {
    Committed(F), Undone(F), Redone(F), NoChange,
    InvalidInput(N), Disallowed(N), Aborted(N),
}
```

From src/behavior.rs:
```rust
pub enum BehaviorResult<D, O> {
    Continue(Vec<D>),
    Stop(O),  // Currently raw O, will become Stop(NonCommittedOutcome<N>)
}

pub trait Behavior<E: EngineSpec> {
    fn name(&self) -> &'static str;
    fn order_key(&self) -> E::OrderKey;
    fn evaluate(&self, input: &E::Input, state: &E::State) -> BehaviorResult<E::Diff, E::NonCommittedInfo>;
}
```

From src/engine.rs (dispatch Stop arm):
```rust
BehaviorResult::Stop(info) => {
    return Ok(Outcome::Aborted(info));  // Hardcoded Aborted — must change
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NonCommittedOutcome and update BehaviorResult contract</name>
  <files>src/outcome.rs, src/behavior.rs, src/engine.rs, src/lib.rs</files>
  <action>
1. In `src/outcome.rs`, add before the `Outcome` enum:
   ```rust
   #[derive(Debug, Clone, PartialEq)]
   pub enum NonCommittedOutcome<N> {
       InvalidInput(N),
       Disallowed(N),
       Aborted(N),
   }
   ```
   Add `From<NonCommittedOutcome<N>> for Outcome<F, N>` impl that maps each variant to the corresponding Outcome variant. Not `#[non_exhaustive]` — three variants are the stable contract.

2. In `src/behavior.rs`:
   - Add `use crate::outcome::NonCommittedOutcome;`
   - Rename type param `O` to `N` in `BehaviorResult<D, N>`
   - Change `Stop(O)` to `Stop(NonCommittedOutcome<N>)`
   - Update the rustdoc example at the top of BehaviorResult to show `BehaviorResult::Stop(NonCommittedOutcome::Aborted("out of bounds".to_string()))` — the example must also `use herdingcats::NonCommittedOutcome;`
   - Update the `StoppingBehavior` in tests to return `BehaviorResult::Stop(NonCommittedOutcome::Aborted("halted".to_string()))` — add `use crate::outcome::NonCommittedOutcome;` to the test module
   - Update `behavior_result_stop_is_constructable_and_matchable` test: the matched value is now `NonCommittedOutcome::Aborted("reason".to_string())`, not just `"reason"`

3. In `src/engine.rs`:
   - Change the dispatch Stop arm from `BehaviorResult::Stop(info) => return Ok(Outcome::Aborted(info))` to `BehaviorResult::Stop(outcome) => return Ok(outcome.into())`
   - Update `use crate::outcome::...` to include `NonCommittedOutcome` if needed by tests
   - Update all test `StopBehavior` impls to return `BehaviorResult::Stop(NonCommittedOutcome::Aborted(...))` — search for every `BehaviorResult::Stop(` in the file
   - Update the rustdoc example on `Engine` struct if it uses `BehaviorResult::Stop`
   - The `stop_halts_dispatch` test assertion `Outcome::Aborted(...)` remains correct (the From impl maps Aborted to Aborted)

4. In `src/lib.rs`:
   - Add `pub use crate::outcome::NonCommittedOutcome;` to the re-exports

Do NOT touch Frame's `reversibility` field — that is Plan 02's job.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test --all 2>&1 | tail -5</automated>
  </verify>
  <done>NonCommittedOutcome enum exists, BehaviorResult::Stop wraps it, dispatch uses .into(), all library tests pass, NonCommittedOutcome re-exported at crate root</done>
</task>

<task type="auto">
  <name>Task 2: Update examples to use NonCommittedOutcome variants semantically</name>
  <files>examples/tictactoe.rs, examples/backgammon.rs</files>
  <action>
1. In `examples/tictactoe.rs`:
   - Add `NonCommittedOutcome` to the `use herdingcats::{...}` import
   - `ValidateTurn`: change `BehaviorResult::Stop("game is over".to_string())` to `BehaviorResult::Stop(NonCommittedOutcome::Disallowed("game is over".to_string()))` — game over is a rule violation
   - `ValidateCell` out-of-bounds: change to `BehaviorResult::Stop(NonCommittedOutcome::InvalidInput("out of bounds".to_string()))` — structurally malformed input
   - `ValidateCell` cell occupied: change to `BehaviorResult::Stop(NonCommittedOutcome::Disallowed("cell already occupied".to_string()))` — valid but forbidden
   - Update behavior doc comments: remove "produces Aborted in dispatch" and replace with the appropriate outcome variant name
   - In `print_dispatch`: remove the comment on InvalidInput arm that says "unreachable via dispatch in MVP engine" — it IS reachable now via out-of-bounds
   - At bottom of main(): remove the line `println!("  InvalidInput — match arm present (unreachable via dispatch in MVP engine)")` and replace with `println!("  InvalidInput — Step 6")` (out-of-bounds now produces InvalidInput)
   - Update Step 3 comment: "Aborted" becomes "Disallowed" (cell occupied is Disallowed)
   - Update Step 6 comment: "Aborted" becomes "InvalidInput" (out-of-bounds is InvalidInput)
   - Update Step 9 comment: "Aborted" becomes "Disallowed" (game over is Disallowed)
   - Update the final summary println block to reflect the new variant mapping

2. In `examples/backgammon.rs`:
   - Add `NonCommittedOutcome` to the `use herdingcats::{...}` import (even though behaviors only use Continue — the type is now in the BehaviorResult signature)
   - Actually: `NonCommittedOutcome` import is NOT needed in backgammon.rs because behaviors only construct `BehaviorResult::Continue(...)` — the type param is inferred. Verify it compiles without the import. Only add it if the compiler requires it.

3. Run both examples to verify they compile and produce correct output.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test --all && cargo run --example tictactoe 2>&1 | tail -15 && cargo run --example backgammon 2>&1 | tail -10</automated>
  </verify>
  <done>tictactoe.rs uses InvalidInput for out-of-bounds, Disallowed for occupied/game-over; backgammon.rs compiles unchanged; both examples run without panics; all tests pass</done>
</task>

</tasks>

<verification>
- `cargo test --all` passes (all unit tests + proptests + doc tests)
- `cargo clippy --all-targets -- -D warnings` passes (no new warnings)
- `cargo run --example tictactoe` runs without panics
- `cargo run --example backgammon` runs without panics
- `NonCommittedOutcome` is importable as `herdingcats::NonCommittedOutcome`
</verification>

<success_criteria>
- BehaviorResult::Stop wraps NonCommittedOutcome<N>, not raw N
- Dispatch Stop arm uses `.into()` via From impl — no hardcoded Aborted
- All 7 Outcome variants remain stable
- tictactoe out-of-bounds produces InvalidInput, not Aborted
- tictactoe cell-occupied and game-over produce Disallowed, not Aborted
- All existing tests updated and passing
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-alignment/05-01-SUMMARY.md`
</output>
