---
phase: 01-core-types
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/behavior.rs
  - src/outcome.rs
  - src/lib.rs
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
must_haves:
  truths:
    - "A library user can implement Behavior<E> for a struct, providing name(), order_key(), and evaluate() — the evaluate signature takes &E::State (immutable borrow), structurally preventing mutation"
    - "BehaviorResult::Continue(vec![]) and BehaviorResult::Stop(x) are constructable and pattern-matchable"
    - "All 7 Outcome variants are constructable and pattern-matchable"
    - "EngineError has exactly 3 variants (BehaviorPanic, InvalidState, CorruptHistory), is marked #[non_exhaustive], and is a distinct type from Outcome"
    - "All public types are accessible at the crate root: use herdingcats::{EngineSpec, Behavior, BehaviorResult, Outcome, Frame, EngineError}"
    - "cargo test passes with zero warnings and cargo clippy -- -D warnings is clean"
  artifacts:
    - path: "src/behavior.rs"
      provides: "Behavior<E> trait and BehaviorResult<D, O> enum"
      exports: ["Behavior", "BehaviorResult"]
      min_lines: 50
    - path: "src/outcome.rs"
      provides: "Outcome<F, N> enum, Frame<E> struct, EngineError enum"
      exports: ["Outcome", "Frame", "EngineError"]
      min_lines: 60
    - path: "src/lib.rs"
      provides: "Flat crate re-exports for all public types"
      exports: ["EngineSpec", "Behavior", "BehaviorResult", "Outcome", "Frame", "EngineError"]
  key_links:
    - from: "src/behavior.rs"
      to: "src/spec.rs"
      via: "use crate::spec::EngineSpec"
      pattern: "use crate::spec::EngineSpec"
    - from: "src/outcome.rs"
      to: "src/spec.rs"
      via: "use crate::spec::EngineSpec"
      pattern: "use crate::spec::EngineSpec"
    - from: "src/lib.rs"
      to: "src/behavior.rs, src/outcome.rs, src/spec.rs"
      via: "pub use re-exports (private mod, not pub mod)"
      pattern: "pub use crate::(spec|behavior|outcome)"
---

<objective>
Implement the remaining four public type contracts for Phase 1: `Behavior<E>`, `BehaviorResult<D, O>`, `Outcome<F, N>`, `Frame<E>`, and `EngineError`. Then wire all types through `src/lib.rs` so the flat `herdingcats::*` API is live. This completes the entire Phase 1 type surface.

Purpose: After this plan, every downstream phase can write `use herdingcats::{EngineSpec, Behavior, Outcome, Frame, EngineError}` and build on a stable, complete API.
Output: `src/behavior.rs`, `src/outcome.rs`, `src/lib.rs` — all with rustdoc and tests.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-types/01-CONTEXT.md
@.planning/phases/01-core-types/01-RESEARCH.md
@.planning/phases/01-core-types/01-01-SUMMARY.md

Key constraint: zero runtime dependencies — `std` only. Rust edition 2024. Implementation modules must be private `mod` (not `pub mod`) in lib.rs.
</context>

<interfaces>
From src/spec.rs (created in Plan 01):

```rust
pub trait EngineSpec {
    type State: Clone + std::fmt::Debug + Default;
    type Input: Clone + std::fmt::Debug;
    type Diff: Clone + std::fmt::Debug;
    type Trace: Clone + std::fmt::Debug;
    type NonCommittedInfo: Clone + std::fmt::Debug;
    type OrderKey: Ord;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Behavior trait and BehaviorResult enum</name>
  <files>src/behavior.rs</files>
  <behavior>
    - Test 1: Define `struct MyBehavior;` and `impl Behavior<TestSpec> for MyBehavior`, providing all 3 methods — code compiles without warnings.
    - Test 2: `evaluate` takes `&self, input: &E::Input, state: &E::State` — attempting to write to `state` inside `evaluate` fails to compile. (Express as `compile_fail` doc-test on the trait.)
    - Test 3: `BehaviorResult::Continue(vec![1u8, 2u8])` is constructable and matches `BehaviorResult::Continue(diffs)` where `diffs == vec![1, 2]`.
    - Test 4: `BehaviorResult::Stop("reason")` is constructable and matches `BehaviorResult::Stop(msg)` where `msg == "reason"`.
  </behavior>
  <action>
Create `src/behavior.rs`. Import `EngineSpec` from `crate::spec`. Define:

1. `BehaviorResult<D, O>` enum with `#[derive(Debug, Clone, PartialEq)]`:
   - `Continue(Vec<D>)` — behavior produced diffs, dispatch continues
   - `Stop(O)` — behavior halted dispatch with non-committed info

2. `Behavior<E: EngineSpec>` trait — dyn-safe:
   - `fn name(&self) -> &'static str` — unique identifier; used as ordering tiebreaker
   - `fn order_key(&self) -> E::OrderKey` — primary sort key for deterministic ordering
   - `fn evaluate(&self, input: &E::Input, state: &E::State) -> BehaviorResult<E::Diff, E::NonCommittedInfo>`

Rustdoc requirements:
- Top-level module doc comment explaining the behavior evaluation contract
- Full rustdoc on `BehaviorResult`, each variant, `Behavior`, each method
- Include a `compile_fail` doc-test on `Behavior::evaluate` showing that `*state = Default::default()` fails to compile (demonstrates structural mutation prevention for CORE-02)
- Include a regular doc-test on `BehaviorResult` showing construction and matching of both variants

Dyn safety invariant: Do NOT add any method-level generic type parameters to `Behavior<E>`. All methods must take only `&self` and types from `E::*`. `Box<dyn Behavior<ConcreteSpec>>` must be valid.

Add `#[cfg(test)]` module at the bottom of the file. Use the same `TestSpec` pattern as Plan 01 (re-define locally or note it's for compile-time verification).

NOTE: `src/lib.rs` is NOT modified in this task. That is Task 3.
  </action>
  <verify>
    <automated>
cd /Users/mprelee/herdingcats && \
  echo 'mod behavior;' >> src/lib.rs && \
  cargo test --lib -- behavior 2>&amp;1 ; \
  STATUS=$? ; \
  sed -i '' '/^mod behavior;$/d' src/lib.rs ; \
  exit $STATUS
    </automated>
  </verify>
  <done>
    `src/behavior.rs` exists. The verify step temporarily appends `mod behavior;` to lib.rs, runs `cargo test --lib -- behavior` (must report tests run, not zero), then removes the line. Tests pass. `Behavior<E>` trait has exactly 3 methods. `BehaviorResult` has `Continue` and `Stop` variants. Full rustdoc including `compile_fail` doc-test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Outcome, Frame, and EngineError</name>
  <files>src/outcome.rs</files>
  <behavior>
    - Test 1: All 7 `Outcome` variants are constructable: `Committed(frame)`, `Undone(frame)`, `Redone(frame)`, `NoChange`, `InvalidInput(nci)`, `Disallowed(nci)`, `Aborted(nci)` — exhaustive match compiles.
    - Test 2: `Frame { input: 42u8, diff: 1u8, trace: "t".to_string() }` is constructable, cloneable, and `==` to an identical frame.
    - Test 3: `EngineError::BehaviorPanic`, `EngineError::InvalidState("msg".into())`, `EngineError::CorruptHistory` are all constructable.
    - Test 4: `EngineError` and `Outcome` are distinct types — confirmed by the compiler (they have different names; no test needed beyond the file compiling).
  </behavior>
  <action>
Create `src/outcome.rs`. Import `EngineSpec` from `crate::spec`. Define:

1. `Frame<E: EngineSpec>` struct with `#[derive(Debug, Clone, PartialEq)]`:
   - `pub input: E::Input`
   - `pub diff: E::Diff`
   - `pub trace: E::Trace`
   - Rustdoc: explain this is the canonical committed record of a single dispatch transition

2. `Outcome<F, N>` enum with `#[must_use = "dispatch outcomes must be handled"]` and `#[derive(Debug, Clone, PartialEq)]`:
   - `Committed(F)` — dispatch succeeded; frame committed to history
   - `Undone(F)` — undo succeeded; frame returned describes undone transition
   - `Redone(F)` — redo succeeded; frame returned describes redone transition
   - `NoChange` — dispatch produced no diffs; nothing committed
   - `InvalidInput(N)` — no behavior accepted the input; semantically invalid
   - `Disallowed(N)` — a behavior explicitly rejected the input
   - `Aborted(N)` — a behavior halted dispatch; no commit
   - Rustdoc: explain the `F = Frame<E>` and `N = E::NonCommittedInfo` convention
   - Do NOT apply `#[non_exhaustive]` to `Outcome` — variants are a stable public contract

3. `EngineError` enum with `#[non_exhaustive]`, `#[derive(Debug, Clone, PartialEq)]`:
   - `BehaviorPanic` — a behavior panicked during evaluation
   - `InvalidState(String)` — engine detected inconsistent internal state
   - `CorruptHistory` — undo/redo history is corrupted
   - Rustdoc: explain `#[non_exhaustive]` semantics — downstream users must add `_ => {}` wildcard

Add `#[cfg(test)]` module at the bottom testing all variants and `Frame` construction.

NOTE: `src/lib.rs` is NOT modified in this task. That is Task 3.
  </action>
  <verify>
    <automated>
cd /Users/mprelee/herdingcats && \
  echo 'mod outcome;' >> src/lib.rs && \
  cargo test --lib -- outcome 2>&amp;1 ; \
  STATUS=$? ; \
  sed -i '' '/^mod outcome;$/d' src/lib.rs ; \
  exit $STATUS
    </automated>
  </verify>
  <done>
    `src/outcome.rs` exists. The verify step temporarily appends `mod outcome;` to lib.rs, runs `cargo test --lib -- outcome` (must report tests run, not zero), then removes the line. Tests pass. All 7 `Outcome` variants present. `Frame<E>` struct has 3 public fields. `EngineError` has 3 variants, is `#[non_exhaustive]`. `#[must_use]` on `Outcome`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire lib.rs re-exports and verify full API surface</name>
  <files>src/lib.rs</files>
  <action>
Overwrite `src/lib.rs` with the private module declarations and `pub use` re-exports:

```rust
//! # HerdingCats
//!
//! A deterministic, turn-based game engine where an ordered set of statically
//! known behaviors resolves every input unambiguously.
//!
//! ## Quick Start
//!
//! 1. Define your game types and bundle them into an [`EngineSpec`] impl.
//! 2. Implement [`Behavior`] for each rule in your game.
//! 3. Construct an `Engine` (Phase 2) and call `dispatch(input, reversibility)`.
//!
//! All public types are re-exported at the crate root.

mod spec;
mod behavior;
mod outcome;

pub use crate::spec::EngineSpec;
pub use crate::behavior::{Behavior, BehaviorResult};
pub use crate::outcome::{EngineError, Frame, Outcome};
```

Modules are private (`mod`, not `pub mod`) — rustdoc inlines documentation automatically. Users write `use herdingcats::{EngineSpec, Behavior, ...}` with no module path.

After writing lib.rs, verify the full test suite. The test filter names used in Tasks 1 and 2 (`behavior`, `outcome`) reference the `#[cfg(test)] mod tests` inside each module. Confirm these pass with the full `cargo test` command.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test 2>&amp;1 && cargo clippy -- -D warnings 2>&amp;1</automated>
  </verify>
  <done>
    `src/lib.rs` declares 3 private modules and re-exports all 6 public items. `cargo test` passes with zero failures. `cargo clippy -- -D warnings` produces no warnings or errors. `cargo doc --no-deps` generates documentation without module duplication artifacts.
  </done>
</task>

</tasks>

<verification>
Overall phase gate (run after all tasks complete):

```bash
cargo test
cargo clippy -- -D warnings
cargo doc --no-deps 2>&1 | grep -i warning || echo "doc clean"
```

All three commands must succeed cleanly.

Manual spot-checks:
- `src/lib.rs` uses `mod` (not `pub mod`) for all three modules
- `EngineError` has `#[non_exhaustive]` attribute
- `Outcome` has `#[must_use]` attribute
- `Outcome` does NOT have `#[non_exhaustive]`
- `Behavior::evaluate` signature is `&self, input: &E::Input, state: &E::State` (no `&mut`)
</verification>

<success_criteria>
- `src/behavior.rs` has `Behavior<E: EngineSpec>` with 3 dyn-safe methods and `BehaviorResult<D, O>` with 2 variants (CORE-02, CORE-03)
- `src/outcome.rs` has `Outcome<F, N>` with 7 variants (#[must_use]), `Frame<E>` with 3 public fields, `EngineError` with 3 variants (#[non_exhaustive]) (CORE-04, CORE-05)
- `src/lib.rs` re-exports all 6 public items from private modules
- `cargo test` passes (zero failures, zero warnings)
- `cargo clippy -- -D warnings` is clean
- Full rustdoc on every public type, trait, variant, and method
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-types/01-02-SUMMARY.md` using the summary template.
</output>
