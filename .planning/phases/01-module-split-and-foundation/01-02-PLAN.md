---
phase: 01-module-split-and-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/hash.rs
  - src/operation.rs
  - src/transaction.rs
  - src/rule.rs
  - src/engine.rs
autonomous: true
requirements: [TEST-01, TEST-03, TEST-04]

must_haves:
  truths:
    - "Every module file has an inline #[cfg(test)] block"
    - "Operation apply+undo roundtrip verified for all three CounterOp variants (Inc, Dec, Reset{prior})"
    - "hash_bytes() returns non-empty Vec<u8> for all variants and identical input gives identical output"
    - "cargo test passes with all new tests green"
  artifacts:
    - path: src/hash.rs
      provides: "#[cfg(test)] block testing fnv1a_hash determinism on raw byte slices"
      contains: "#[cfg(test)]"
    - path: src/operation.rs
      provides: "#[cfg(test)] block"
      contains: "#[cfg(test)]"
    - path: src/transaction.rs
      provides: "#[cfg(test)] block testing Transaction::new() defaults"
      contains: "#[cfg(test)]"
    - path: src/rule.rs
      provides: "#[cfg(test)] block testing Rule trait object contract"
      contains: "#[cfg(test)]"
    - path: src/engine.rs
      provides: "#[cfg(test)] block with CounterOp fixture and apply+undo roundtrip tests"
      contains: "CounterOp"
  key_links:
    - from: engine.rs #[cfg(test)]
      to: CounterOp fixture
      via: use super::*
      pattern: "CounterOp"
---

<objective>
Add inline #[cfg(test)] modules to all five source files. The engine.rs test module gets the full counter fixture (CounterOp with Inc, Dec, Reset{prior:i32}) and apply+undo roundtrip tests. Other modules test their own narrow slice in isolation.

Purpose: Satisfies TEST-01 (every module has tests), TEST-03 (apply+undo roundtrip), TEST-04 (hash_bytes determinism).
Output: Five #[cfg(test)] blocks, all cargo test passes green.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-module-split-and-foundation/01-CONTEXT.md
@.planning/phases/01-module-split-and-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Counter fixture design decision (from RESEARCH.md open question, resolved here) -->
<!-- Use Reset { prior: i32 } to make undo correct and maximally illustrative. -->

// CounterOp fixture — defined inline in engine.rs #[cfg(test)] only
#[derive(Clone, Debug, PartialEq)]
enum CounterOp {
    Inc,
    Dec,
    Reset { prior: i32 },  // stores prior value so undo can restore exactly
}

impl Operation<i32> for CounterOp {
    fn apply(&self, state: &mut i32) {
        match self {
            CounterOp::Inc => *state += 1,
            CounterOp::Dec => *state -= 1,
            CounterOp::Reset { .. } => *state = 0,
        }
    }
    fn undo(&self, state: &mut i32) {
        match self {
            CounterOp::Inc => *state -= 1,
            CounterOp::Dec => *state += 1,
            CounterOp::Reset { prior } => *state = *prior,
        }
    }
    fn hash_bytes(&self) -> Vec<u8> {
        match self {
            CounterOp::Inc              => vec![0],
            CounterOp::Dec              => vec![1],
            CounterOp::Reset { prior }  => {
                let mut v = vec![2];
                v.extend_from_slice(&prior.to_le_bytes());
                v
            }
        }
    }
}

struct NoRule;
impl Rule<i32, CounterOp, (), u8> for NoRule {
    fn id(&self) -> &'static str { "no_rule" }
    fn priority(&self) -> u8 { 0 }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests for hash.rs, operation.rs, transaction.rs, rule.rs</name>
  <files>src/hash.rs, src/operation.rs, src/transaction.rs, src/rule.rs</files>
  <behavior>
    - hash.rs: fnv1a_hash(b"hello") == fnv1a_hash(b"hello") (determinism)
    - hash.rs: fnv1a_hash(b"hello") != fnv1a_hash(b"world") (sensitivity)
    - hash.rs: fnv1a_hash(&[]) returns FNV_OFFSET (empty input is defined)
    - transaction.rs: Transaction::new() has ops empty, irreversible true, deterministic true, cancelled false
    - transaction.rs: Transaction::new() cancelled field is false
    - rule.rs: A minimal Rule impl compiles and id()/priority() return expected values
    - operation.rs: A minimal Operation impl (Inc on i32) compiles and apply/undo invert
  </behavior>
  <action>
Append a `#[cfg(test)] mod tests { use super::*; ... }` block at the bottom of each file, preceded by the section banner:

```rust
// ============================================================
// Tests
// ============================================================

#[cfg(test)]
mod tests {
    use super::*;
    // ... tests for this module's items only
}
```

**hash.rs tests:** Test fnv1a_hash directly on byte slices (use super::* gives access to pub(crate) fn):
- `hash_determinism`: assert_eq!(fnv1a_hash(b"hello"), fnv1a_hash(b"hello"))
- `hash_sensitivity`: assert_ne!(fnv1a_hash(b"hello"), fnv1a_hash(b"world"))
- `hash_empty_input`: assert_eq!(fnv1a_hash(&[]), FNV_OFFSET)

**operation.rs tests:** Define a minimal inline Op (just Inc on i32) and test apply+undo invert. This tests the trait object pattern compiles correctly. Keep it minimal — the full fixture is in engine.rs.

**transaction.rs tests:** Test Transaction::new() default field values (exact assertions from the interface block above). Test that cancelled can be set to true.

**rule.rs tests:** Define a minimal Rule impl inline (unit struct, id returns "test", priority returns 0u8), construct it, verify id() and priority() return expected values. Confirms the trait object pattern compiles.

No cross-module imports in test modules — each file tests only its own slice with `use super::*`.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test hash transaction rule operation 2>&1</automated>
  </verify>
  <done>Four #[cfg(test)] blocks exist (one per file). cargo test with hash/transaction/rule/operation filter shows all tests passing. No test imports from sibling modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Counter fixture and engine tests</name>
  <files>src/engine.rs</files>
  <behavior>
    - Inc: apply to 0 → 1, undo → 0 (roundtrip restores exactly)
    - Dec: apply to 0 → -1, undo → 0 (roundtrip restores exactly)
    - Reset{prior:5}: apply to 5 → 0, undo → 5 (roundtrip restores prior value)
    - hash_bytes(Inc) is non-empty
    - hash_bytes(Inc) == hash_bytes(Inc) (determinism)
    - hash_bytes(Inc) != hash_bytes(Dec) (variant uniqueness)
    - hash_bytes(Inc) != hash_bytes(Reset{prior:0}) (variant uniqueness)
    - Engine::new(0i32) with no rules: dispatch then undo restores state to 0
  </behavior>
  <action>
Append a `#[cfg(test)] mod tests { use super::*; ... }` block at the bottom of src/engine.rs, preceded by the section banner.

Include the full CounterOp fixture as defined in the interfaces block above (Inc, Dec, Reset{prior:i32} with correct apply/undo/hash_bytes implementations).

Write these tests:

**apply+undo roundtrip (TEST-03) — one test per variant:**
```
apply_undo_inc: start=0, apply Inc, assert state==1, undo, assert state==0
apply_undo_dec: start=0, apply Dec, assert state==-1, undo, assert state==0
apply_undo_reset: start=5, create Reset{prior:5}, apply, assert state==0, undo, assert state==5
```

**hash_bytes determinism and non-empty (TEST-04):**
```
hash_bytes_nonempty: assert !CounterOp::Inc.hash_bytes().is_empty()
hash_bytes_determinism: assert_eq!(CounterOp::Inc.hash_bytes(), CounterOp::Inc.hash_bytes())
hash_bytes_variant_uniqueness: Inc != Dec, Inc != Reset{prior:0}
```

**Engine integration (basic smoke — full property tests come in Phase 2):**
```
engine_dispatch_undo: Engine::new(0i32), add NoRule, dispatch tx with Inc, assert state==1, undo(), assert state==0
```

Test style: Use `assert_eq!` and `assert_ne!`. No `unwrap()` in test code either — but calling methods that return values directly (not Results) is fine.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test 2>&1</automated>
  </verify>
  <done>src/engine.rs has a #[cfg(test)] block with CounterOp fixture. All five test behaviors pass. cargo test shows all tests green across all modules. cargo test --examples still passes.</done>
</task>

</tasks>

<verification>
```
cargo test 2>&1
cargo test --examples 2>&1
```

Confirm:
- Tests exist in all five module files (grep -l "#\[cfg(test)\]" src/*.rs | wc -l = 6, since lib.rs may also have one, or 5 if not)
- apply+undo roundtrip tests cover all three CounterOp variants
- hash_bytes tests cover determinism and non-empty for all variants
- No test failures
</verification>

<success_criteria>
1. Every source file (hash.rs, operation.rs, transaction.rs, rule.rs, engine.rs) has an inline #[cfg(test)] block
2. CounterOp fixture uses Reset{prior:i32} — undo is correct for all variants
3. apply+undo roundtrip test exists for Inc, Dec, and Reset{prior}
4. hash_bytes determinism + non-empty tested for all variants
5. cargo test passes (all tests green)
6. cargo test --examples still passes (tictactoe unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/01-module-split-and-foundation/01-02-SUMMARY.md`
</output>
