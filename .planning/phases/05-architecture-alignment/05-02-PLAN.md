---
phase: 05-architecture-alignment
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/apply.rs
  - src/outcome.rs
  - src/engine.rs
autonomous: true
requirements: [SC-2, SC-4]

must_haves:
  truths:
    - "Frame<E> has only input, diffs, traces fields — no reversibility"
    - "Reversibility is stored in undo_stack/redo_stack tuples as the third element"
    - "Apply trait docs state each call MUST return at least one trace entry"
    - "All tests referencing frame.reversibility are updated or removed"
    - "cargo test --all passes after Frame shape change"
  artifacts:
    - path: "src/outcome.rs"
      provides: "Frame<E> with 3 fields only (input, diffs, traces)"
      contains: "pub struct Frame"
    - path: "src/engine.rs"
      provides: "undo_stack/redo_stack as Vec<(E::State, Frame<E>, Reversibility)>"
      contains: "Vec<(E::State, Frame<E>, Reversibility)>"
    - path: "src/apply.rs"
      provides: "Updated doc contract requiring at least one trace entry"
      contains: "MUST return at least one trace entry"
  key_links:
    - from: "src/engine.rs"
      to: "src/outcome.rs"
      via: "Frame construction without reversibility"
      pattern: "Frame \\{\\s*input"
    - from: "src/engine.rs"
      to: "src/engine.rs"
      via: "undo_stack tuple now includes Reversibility"
      pattern: "undo_stack\\.push\\("
---

<objective>
Remove reversibility from Frame<E> and move it to history stack tuples. Update Apply trait docs to enforce trace contract.

Purpose: Frame should be a pure record of {input, diffs, traces} per ARCHITECTURE.md. Reversibility is dispatch metadata, not frame data.
Output: Clean Frame struct, updated stacks, updated trace docs, all tests green.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-architecture-alignment/05-CONTEXT.md
@.planning/phases/05-architecture-alignment/05-RESEARCH.md
@.planning/phases/05-architecture-alignment/05-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these are the relevant types. -->

From src/outcome.rs (post Plan 01):
```rust
pub struct Frame<E: EngineSpec> {
    pub input: E::Input,
    pub diffs: Vec<E::Diff>,
    pub traces: Vec<E::Trace>,
    pub reversibility: Reversibility,  // TO BE REMOVED in this plan
}
// Manual Clone and PartialEq impls include reversibility — must update
```

From src/engine.rs (post Plan 01):
```rust
pub struct Engine<E: EngineSpec> {
    state: E::State,
    behaviors: Vec<Box<dyn Behavior<E>>>,
    undo_stack: Vec<(E::State, Frame<E>)>,      // becomes 3-tuple
    redo_stack: Vec<(E::State, Frame<E>)>,      // becomes 3-tuple
}
```

Key dispatch construction site:
```rust
let frame = Frame { input, diffs, traces, reversibility };  // remove reversibility
self.undo_stack.push((prior_state, frame.clone()));          // add reversibility to tuple
```

Key undo/redo sites:
```rust
Some((prior_state, frame)) => { ... }  // becomes (prior_state, frame, _rev)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Apply docs + remove reversibility from Frame + update stacks</name>
  <files>src/apply.rs, src/outcome.rs, src/engine.rs</files>
  <action>
1. In `src/apply.rs`:
   - Find the doc comment on the `apply` method: `/// Returning an empty \`Vec\` is valid for diffs that produce no trace.`
   - Replace with: `/// Each call MUST return at least one trace entry describing the mutation. The engine does not accept empty trace from a state-mutating diff.`

2. In `src/outcome.rs`:
   - Remove `pub reversibility: Reversibility` field from `Frame<E>`
   - Remove `use crate::reversibility::Reversibility;` import (Reversibility is no longer used in this file)
   - Update the Frame struct doc comment to remove mention of reversibility
   - Update the manual `Clone` impl: remove `reversibility: self.reversibility` from the clone body
   - Update the manual `PartialEq` impl: remove `&& self.reversibility == other.reversibility` from the eq body
   - Update tests in the `mod tests` block:
     - `make_frame()`: remove `reversibility: Reversibility::Reversible` from Frame construction. Also remove `use crate::reversibility::Reversibility;` from the test imports if it becomes unused (keep it if other tests still need it for non-Frame purposes — check).
     - `frame_is_constructable_cloneable_and_eq`: remove `assert_eq!(f.reversibility, Reversibility::Reversible);`
     - `frame_stores_vec_diffs_and_vec_traces`: remove `reversibility: Reversibility::Irreversible` from Frame construction
     - `frame_stores_reversibility` test: DELETE this entire test — the concept no longer applies to Frame
     - Remove unused `Reversibility` import from test module if no tests reference it anymore

3. In `src/engine.rs`:
   - Change struct field types: `undo_stack: Vec<(E::State, Frame<E>, Reversibility)>` and same for `redo_stack`
   - In `Engine::new()`: stack initialization stays `Vec::new()` — no change needed
   - In `dispatch()`:
     - Frame construction: remove `reversibility` field (`Frame { input, diffs, traces }`)
     - Change `self.undo_stack.push((prior_state, frame.clone()))` to `self.undo_stack.push((prior_state, frame.clone(), reversibility))`
     - The irreversibility check `if reversibility == Reversibility::Irreversible` stays the same — it reads the local variable
   - In `undo()`:
     - Change `Some((prior_state, frame))` to `Some((prior_state, frame, reversibility))`
     - Change `self.redo_stack.push((current_state, frame.clone()))` to `self.redo_stack.push((current_state, frame.clone(), reversibility))`
   - In `redo()`:
     - Change `Some((prior_state, frame))` to `Some((prior_state, frame, reversibility))`
     - Change `self.undo_stack.push((current_state, frame.clone()))` to `self.undo_stack.push((current_state, frame.clone(), reversibility))`
   - Update the Engine rustdoc example if it constructs a Frame with reversibility
   - In tests: search for all `frame.reversibility` assertions and Frame constructions with `reversibility:` — remove/update each one:
     - `frame_contains_input_diffs_trace`: remove `assert_eq!(frame.reversibility, Reversibility::Irreversible)` (or whatever assertion exists)
     - Any other test constructing Frame directly must drop the reversibility field
   - Verify no other test references `frame.reversibility`
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test --all && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Frame has 3 fields only; stacks are 3-tuples with Reversibility; Apply doc enforces trace contract; all tests and clippy pass; no unused imports</done>
</task>

</tasks>

<verification>
- `cargo test --all` passes
- `cargo clippy --all-targets -- -D warnings` passes (no unused import warnings)
- `cargo run --example tictactoe` runs without panics
- `cargo run --example backgammon` runs without panics
- Frame struct has exactly 3 public fields: input, diffs, traces
- No file references `frame.reversibility`
</verification>

<success_criteria>
- Frame<E> contains only input, diffs, traces — no reversibility field
- undo_stack and redo_stack are Vec<(E::State, Frame<E>, Reversibility)>
- Apply trait docs state "MUST return at least one trace entry"
- All tests pass with updated Frame construction and assertions
- No unused import warnings from clippy
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-alignment/05-02-SUMMARY.md`
</output>
