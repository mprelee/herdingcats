---
phase: 06-validation-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase5_codegen.rs
  - tests/phase6_validation.rs
  - examples/dsl_consumer/build.rs
  - examples/dsl_consumer/rules/example.cats
  - examples/dsl_consumer/src/main.rs
  - crates/herdingcats_codegen/src/lib.rs
  - crates/herdingcats_codegen/src/codegen.rs
autonomous: true
requirements: [GEN-04, INT-03, INT-04]

must_haves:
  truths:
    - "Generated-rule validation reuses the repository's existing invariant contract: undo/cancel checks must assert both state and replay hash"
    - "Deterministic generated operations are validated through behavior-level tests on generated op values and generated-rule dispatch, not just source-string equality"
    - "Invalid authored DSL fails through a real nested consumer-build path with stable, actionable stderr assertions"
    - "Phase 6 validation stays focused on fixed authored fixtures and real generated output instead of parser fuzzing or new runtime APIs"
  artifacts:
    - path: tests/phase6_validation.rs
      provides: generated-op determinism, generated-rule property tests, and failing-build diagnostics coverage
    - path: tests/phase5_codegen.rs
      provides: updated nested-consumer fixtures and shared integration helpers for generated-rule validation
    - path: examples/dsl_consumer/rules/example.cats
      provides: canonical authored DSL fixture reused by the validation harness
  key_links:
    - from: tests/phase6_validation.rs
      to: src/engine.rs
      via: generated-rule invariant tests must mirror the established undo/replay-hash property style
      pattern: "replay_hash|undo"
    - from: tests/phase6_validation.rs
      to: examples/dsl_consumer/build.rs
      via: diagnostics coverage must exercise the real build.rs integration path
      pattern: "cargo|OUT_DIR"
---

<objective>
Build the Phase 6 validation suite for generated rules and generated operations.

Purpose: prove the Phase 5 DSL pipeline preserves the engine's deterministic apply/undo/replay-hash contract and that invalid authored inputs fail through the real consumer integration path with actionable diagnostics.
Output: invariant-focused integration tests plus a failing-build diagnostics harness.
</objective>

<execution_context>
@/Users/mprelee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-validation-and-release-hardening/06-RESEARCH.md
@.planning/phases/06-validation-and-release-hardening/06-VALIDATION.md
@.planning/phases/05-build-time-compiler-and-example-integration/05-03-SUMMARY.md
@tests/phase5_codegen.rs
@src/engine.rs
@examples/backgammon.rs
@examples/dsl_consumer/build.rs
@examples/dsl_consumer/rules/example.cats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic generated-op and generated-rule invariant coverage</name>
  <files>tests/phase6_validation.rs, tests/phase5_codegen.rs, examples/dsl_consumer/rules/example.cats, examples/dsl_consumer/src/main.rs, crates/herdingcats_codegen/src/codegen.rs</files>
  <action>
Create a dedicated Phase 6 validation test target that exercises generated output through the real compiler path and mirrors the repository's existing invariant style.

Required outcomes:
- add focused tests proving identical generated op payloads produce identical `hash_bytes()` output and stable replay-hash behavior
- add direct op-level roundtrip tests for generated operations that apply generated ops, undo them, and assert both state restoration and replay-hash restoration where the engine contract applies
- add at least one generated-rule property test that dispatches real generated rules, undoes committed work, and asserts both `engine.read()` and `engine.replay_hash()` return to their pre-dispatch values
- add targeted cancellation or non-committing-path tests that prove generated `cancel`/no-op behavior leaves both state and replay hash unchanged
- keep authored fixtures fixed and small; drive variability through event/op values in the test harness instead of generating arbitrary DSL text

Scope guard:
- no new runtime engine traits or public API redesign
- no parser fuzzing framework
- no docs work in this task
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test --test phase6_validation</automated>
  </verify>
  <done>`tests/phase6_validation.rs` proves deterministic generated-op hashing and generated-rule undo/replay-hash invariants using the established repository test style.</done>
</task>

<task type="auto">
  <name>Task 2: Add failing-build diagnostics coverage for invalid authored DSL</name>
  <files>tests/phase6_validation.rs, tests/phase5_codegen.rs, examples/dsl_consumer/build.rs, crates/herdingcats_codegen/src/lib.rs</files>
  <action>
Extend the integration harness so invalid authored DSL is tested through a real nested consumer-build path and asserted by stderr substrings rather than panic snapshots.

Required outcomes:
- add one or more failing nested-consumer fixtures or temporary-build cases run from integration tests with `std::process::Command`
- cover at least syntax failure plus one semantic failure from the locked v1.1 contract such as duplicate ids, unapproved bindings, or unsupported semantics
- assert diagnostics are actionable: the stderr must identify the failing file or rule context, the invalid construct, and a corrective direction or safe alternative
- keep assertions stable by matching important substrings, not full stderr snapshots

Design bar:
- reuse the existing nested Cargo invocation style from `tests/phase5_codegen.rs`
- make the harness practical for later diagnostic-polish work in Plan 06-02 instead of hard-coding brittle output
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test diagnostics -- --nocapture</automated>
  </verify>
  <done>The repo has real failing-build coverage proving invalid authored DSL surfaces actionable diagnostics through the consumer integration path.</done>
</task>

</tasks>

<verification>
Final gate after both tasks:

```bash
cargo test --test phase6_validation
cargo test --test phase5_codegen
cargo test generated_rule -- --nocapture
test -f tests/phase6_validation.rs
```

Confirm:
- generated-op determinism is asserted at the operation/engine behavior level
- generated-op roundtrip coverage explicitly proves apply/undo restoration rather than inferring it only through rule dispatch
- generated-rule undo coverage checks both state restoration and replay-hash restoration
- invalid DSL failures are covered through the real nested consumer-build path
- no new runtime dependency or trait surface was introduced to satisfy the tests
</verification>

<success_criteria>
1. Generated-operation tests directly prove stable deterministic behavior required by `GEN-04`
2. Generated-operation and generated-rule validation together prove apply/undo and replay-hash invariants required by `INT-04`
3. Invalid authored DSL is covered by failing-build integration tests with actionable diagnostics for `INT-03`
4. The Phase 6 validation harness is built on real generated fixtures and existing repository test patterns
</success_criteria>

<output>
After completion, create `.planning/phases/06-validation-and-release-hardening/06-01-SUMMARY.md`
</output>
