---
phase: 06-validation-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on: [06-01-PLAN.md]
files_modified:
  - crates/herdingcats_codegen/src/diagnostics.rs
  - crates/herdingcats_codegen/src/lib.rs
  - crates/herdingcats_codegen/src/validate.rs
  - tests/phase5_codegen.rs
  - README.md
  - docs/dsl/README.md
  - src/lib.rs
autonomous: true
requirements: [INT-03, INT-05]

must_haves:
  truths:
    - "Diagnostics polish must improve the actual build-time user experience without weakening the semantic rejection rules proven in 06-01"
    - "Handwritten-only `herdingcats` usage remains first-class: existing examples and library consumers must still compile when the DSL path is unused"
    - "Release docs explicitly state the v1.1 boundary: build-time generation only, no runtime parser/runtime scripting, and no broad generated after() mutation support"
    - "The runtime crate's public surface remains additive and codegen stays on the build-time side of the boundary"
  artifacts:
    - path: crates/herdingcats_codegen/src/diagnostics.rs
      provides: richer diagnostic formatting and structured authored-context reporting for build failures
    - path: README.md
      provides: top-level release-facing explanation of DSL boundaries and handwritten-only compatibility
    - path: docs/dsl/README.md
      provides: consumer-facing integration guidance for the build-time codegen path
  key_links:
    - from: crates/herdingcats_codegen/src/diagnostics.rs
      to: .planning/phases/06-validation-and-release-hardening/06-VALIDATION.md
      via: diagnostic output must satisfy the actionable-feedback bar defined for Phase 6
      pattern: "file|rule|help"
    - from: README.md
      to: examples/tictactoe.rs
      via: release docs must preserve the non-DSL usage path as a first-class story
      pattern: "example|handwritten"
---

<objective>
Harden the Phase 6 release surface: polish diagnostics, prove handwritten-only compatibility, and make the public v1.1 boundary explicit in docs and crate messaging.

Purpose: finish the release-facing work after the validation harness exists, so the shipped DSL path is easier to diagnose, does not regress non-DSL consumers, and clearly documents what v1.1 does and does not support.
Output: structured diagnostics improvements, compatibility checks, and updated release docs.
</objective>

<execution_context>
@/Users/mprelee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-validation-and-release-hardening/06-RESEARCH.md
@.planning/phases/06-validation-and-release-hardening/06-VALIDATION.md
@.planning/phases/06-validation-and-release-hardening/06-01-PLAN.md
@README.md
@docs/dsl/README.md
@src/lib.rs
@examples/tictactoe.rs
@examples/backgammon.rs
@examples/dsl_consumer/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish diagnostics and lock compatibility coverage</name>
  <files>crates/herdingcats_codegen/src/diagnostics.rs, crates/herdingcats_codegen/src/lib.rs, crates/herdingcats_codegen/src/validate.rs, tests/phase5_codegen.rs</files>
  <action>
Improve the build-time diagnostics shape proven by Plan 06-01 and add explicit compatibility checks for the handwritten-only path.

Required outcomes:
- extend diagnostics formatting so build failures carry stable authored context such as source path, rule id or failing construct, and short help text when available
- make `build.rs`-surfaced errors readable and assertable without relying on opaque panic text
- add compatibility checks that prove the root library and existing examples still compile/run without any DSL setup
- keep codegen on the build-time boundary; do not introduce runtime-crate dependencies on parser/codegen internals

Compatibility bar:
- `cargo test --lib --examples` must stay green
- `cargo run --example tictactoe` and `cargo run --example backgammon` must stay green
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test --lib --examples</automated>
  </verify>
  <done>Diagnostics are more actionable and the handwritten-only library/examples are explicitly covered as unchanged by the DSL addition.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/examples messaging and review the public boundary</name>
  <files>README.md, docs/dsl/README.md, src/lib.rs</files>
  <action>
Update release-facing documentation so the v1.1 scope is clear to downstream users and matches the implemented architecture.

Required outcomes:
- top-level docs explain that DSL support is build-time generation only and not a runtime parser or scripting system
- docs explicitly state that broad generated mutating `after()` behavior is out of scope in v1.1
- docs preserve a first-class handwritten-only usage story and point to existing examples that continue to work unchanged
- crate-level docs or public-facing module docs do not imply a broader API boundary than the implementation actually supports

Review bar:
- verify the runtime crate's public docs do not accidentally expose codegen as part of the normal runtime API
- verify the DSL docs reflect the real `build.rs` + `OUT_DIR` consumer path from Phase 5
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && rg "build-time|runtime parser|runtime scripting|after\\(\\)|handwritten" README.md docs/dsl/README.md src/lib.rs</automated>
  </verify>
  <done>Release docs and public crate messaging make the DSL boundary explicit while preserving the unchanged handwritten-only path.</done>
</task>

</tasks>

<verification>
Final gate after both tasks:

```bash
cargo test
cargo test --lib --examples
cargo run --example tictactoe
cargo run --example backgammon
cargo run --quiet --manifest-path examples/dsl_consumer/Cargo.toml
rg "build-time|runtime parser|runtime scripting|after\\(\\)|handwritten" README.md docs/dsl/README.md src/lib.rs
```

Confirm:
- diagnostic output is actionable enough to identify the failing authored input and a corrective direction
- handwritten-only usage still works with no DSL setup or feature toggles
- the DSL path remains build-time only and documented as such
- public-facing docs do not imply runtime scripting or broad generated `after()` mutation support
</verification>

<success_criteria>
1. Diagnostics polish closes the actionable-feedback gap for `INT-03` without changing the semantic contract
2. Compatibility checks prove the existing handwritten library and examples still compile/run for `INT-05`
3. README, DSL docs, and crate docs accurately describe the build-time-only v1.1 boundary
4. The runtime/codegen architectural boundary remains additive and release-ready
</success_criteria>

<output>
After completion, create `.planning/phases/06-validation-and-release-hardening/06-02-SUMMARY.md`
</output>
