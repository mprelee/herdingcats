---
phase: 04-core-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mutation.rs
  - src/behavior.rs
  - src/action.rs
  - src/lib.rs
autonomous: true
requirements:
  - REN-01
  - REN-02
  - REN-03

must_haves:
  truths:
    - "src/mutation.rs exists and exports a public trait Mutation<S> with apply, undo, hash_bytes"
    - "src/behavior.rs exists and exports a public trait Behavior<S,M,I,P> with before, after, id, priority"
    - "src/action.rs exists and exports a public struct Action<M> with fields mutations, deterministic, cancelled (no irreversible field, no RuleLifetime)"
    - "src/lib.rs re-exports Mutation, Behavior, Action (not Operation, Rule, Transaction, RuleLifetime)"
    - "The old module names are no longer declared in lib.rs"
  artifacts:
    - path: "src/mutation.rs"
      provides: "Mutation<S> trait definition"
      exports: ["Mutation"]
    - path: "src/behavior.rs"
      provides: "Behavior<S,M,I,P> trait definition"
      exports: ["Behavior"]
    - path: "src/action.rs"
      provides: "Action<M> struct definition"
      exports: ["Action"]
    - path: "src/lib.rs"
      provides: "Public API re-exports"
      contains: "pub use mutation::Mutation"
  key_links:
    - from: "src/lib.rs"
      to: "src/mutation.rs"
      via: "mod mutation"
      pattern: "mod mutation"
    - from: "src/behavior.rs"
      to: "src/mutation.rs"
      via: "use crate::mutation::Mutation"
      pattern: "use crate::mutation"
    - from: "src/behavior.rs"
      to: "src/action.rs"
      via: "use crate::action::Action"
      pattern: "use crate::action"
---

<objective>
Create the three new public trait/struct modules with their renamed definitions, and update lib.rs to declare the new modules and re-export the new names.

Purpose: Establish the contracts (Mutation, Behavior, Action) that the engine and examples will be updated to use in subsequent plans. This is the interface-first step — get the names right before wiring the engine.

Output: src/mutation.rs, src/behavior.rs, src/action.rs (new files with new names), src/lib.rs (updated module list and re-exports). Old files (operation.rs, rule.rs, transaction.rs) are NOT deleted yet — engine.rs still references them and will be updated in Plan 02.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current source files being renamed. Executor reads these for content to port. -->

From src/operation.rs (current):
```rust
pub trait Operation<S>: Clone {
    fn apply(&self, state: &mut S);
    fn undo(&self, state: &mut S);
    fn hash_bytes(&self) -> Vec<u8>;
}
```

From src/rule.rs (current):
```rust
use crate::operation::Operation;
use crate::transaction::Transaction;

pub trait Rule<S, O, E, P>
where
    S: Clone,
    O: Operation<S>,
    P: Copy + Ord,
{
    fn id(&self) -> &'static str;
    fn priority(&self) -> P;
    fn before(&self, _state: &S, _event: &mut E, _tx: &mut Transaction<O>) {}
    fn after(&self, _state: &S, _event: &E, _tx: &mut Transaction<O>) {}
}
```

From src/transaction.rs (current):
```rust
pub struct Transaction<O> {
    pub ops: Vec<O>,
    pub irreversible: bool,
    pub deterministic: bool,
    pub cancelled: bool,
}
// Also contains: pub enum RuleLifetime { Permanent, Turns(u32), Triggers(u32) }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/mutation.rs (Operation -> Mutation)</name>
  <files>src/mutation.rs</files>
  <action>
Create src/mutation.rs as a new file. Port ALL content from src/operation.rs with these exact changes:

1. Rename the trait from `Operation<S>` to `Mutation<S>` everywhere — trait declaration, impl blocks in tests, doc comments, and all doctests embedded in the doc comments.

2. In the doctests within the doc comments: replace `use herdingcats::Operation` with `use herdingcats::Mutation`, replace `impl Operation<i32> for CounterOp` with `impl Mutation<i32> for CounterOp`, and rename any variable names like `let op =` to `let m =` for clarity but only where they appear in example code — the method names (`apply`, `undo`, `hash_bytes`) are PRESERVED unchanged.

3. In the test module at the bottom: rename `impl Operation<i32> for Inc` to `impl Mutation<i32> for Inc`. Rename the test function `operation_apply_undo_invert` to `mutation_apply_undo_invert`.

4. Do NOT change method signatures: `apply`, `undo`, `hash_bytes` remain exactly the same.

5. Do NOT add any new methods (is_reversible belongs to Phase 5).

The file should have no references to `Operation` anywhere — only `Mutation`.
  </action>
  <verify>
    <automated>grep -n "Operation" /Users/mprelee/herdingcats/src/mutation.rs; grep -c "Mutation" /Users/mprelee/herdingcats/src/mutation.rs</automated>
  </verify>
  <done>src/mutation.rs exists, contains `pub trait Mutation<S>: Clone`, zero occurrences of "Operation", methods apply/undo/hash_bytes present with identical signatures as before.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/behavior.rs (Rule -> Behavior) and src/action.rs (Transaction -> Action)</name>
  <files>src/behavior.rs, src/action.rs</files>
  <action>
**src/action.rs — Create from src/transaction.rs with these changes:**

1. Rename struct `Transaction<O>` to `Action<M>`.
2. Rename field `ops: Vec<O>` to `mutations: Vec<M>`. (The type parameter `O` becomes `M` throughout.)
3. REMOVE the `irreversible` field entirely — it does not appear in `Action<M>`. The struct has exactly three fields: `mutations`, `deterministic`, `cancelled`.
4. REMOVE the entire `RuleLifetime` enum and all associated doc comments — it must not exist anywhere in action.rs.
5. Update `Action::new()` to initialize `mutations: vec![]` (no `irreversible` initialization). Update doc comment to reflect new field names and no `irreversible` field.
6. Update `Default` impl for `Action<M>`.
7. Update test module: rename `Transaction` to `Action`, `ops` to `mutations`, remove any test that asserts on `irreversible` field (or update it — the `transaction_new_defaults` test should become `action_new_defaults` and only test `mutations.is_empty()`, `deterministic`, `cancelled`). The test `transaction_cancelled_can_be_set` becomes `action_cancelled_can_be_set`.
8. All doctests in doc comments: update `use herdingcats::Transaction` to `use herdingcats::Action`, `Transaction::new()` to `Action::new()`, `tx.ops` to `tx.mutations`. Remove any doctest assertion on `tx.irreversible`.
9. The file must have ZERO occurrences of: `Transaction`, `irreversible`, `RuleLifetime`, `Permanent`, `Turns`, `Triggers`.

**src/behavior.rs — Create from src/rule.rs with these changes:**

1. Change imports: `use crate::mutation::Mutation;` and `use crate::action::Action;` (remove old operation/transaction imports).
2. Rename trait `Rule<S, O, E, P>` to `Behavior<S, M, I, P>`. Type parameter `E` is renamed to `I` (for Input). `O` is renamed to `M` (for Mutation). The where clause becomes: `S: Clone`, `M: Mutation<S>`, `P: Copy + Ord`.
3. Method signatures: `before` and `after` receive `Action<M>` instead of `Transaction<O>`. Specifically:
   - `fn before(&self, _state: &S, _event: &mut I, _tx: &mut Action<M>) {}`
   - `fn after(&self, _state: &S, _event: &I, _tx: &mut Action<M>) {}`
4. Method `id` and `priority` signatures are PRESERVED unchanged.
5. Do NOT add is_active, on_dispatch, on_undo — those belong to Phase 5.
6. Update ALL doc comments and doctests:
   - `use herdingcats::{Operation, Rule, Transaction, RuleLifetime}` → `use herdingcats::{Mutation, Behavior, Action}`
   - `impl Operation<i32> for CounterOp` → `impl Mutation<i32> for CounterOp`
   - `impl Rule<i32, CounterOp, (), u8> for DoubleRule` → `impl Behavior<i32, CounterOp, (), u8> for DoubleRule`
   - `tx: &mut Transaction<CounterOp>` → `tx: &mut Action<CounterOp>`
   - `tx.ops` → `tx.mutations`
   - Remove all `RuleLifetime` from doctest imports
7. In test module: rename `NoOp` impl from `impl Operation<()> for NoOp` to `impl Mutation<()> for NoOp`. Rename `impl Rule<...> for TestRule` to `impl Behavior<...> for TestRule`. The where bound changes: `Rule<(), NoOp, (), u8>` → `Behavior<(), NoOp, (), u8>`. Test name `rule_id_and_priority` → `behavior_id_and_priority`.
8. The file must have ZERO occurrences of: `Rule`, `Operation`, `Transaction`, `RuleLifetime`.
  </action>
  <verify>
    <automated>grep -n "Transaction\|RuleLifetime\|irreversible\|Rule\b\|Operation\b" /Users/mprelee/herdingcats/src/action.rs /Users/mprelee/herdingcats/src/behavior.rs 2>&1 | head -30</automated>
  </verify>
  <done>src/action.rs has struct Action&lt;M&gt; with fields mutations/deterministic/cancelled only. src/behavior.rs has trait Behavior&lt;S,M,I,P&gt; with before/after/id/priority. Zero occurrences of old names in either file.</done>
</task>

<task type="auto">
  <name>Task 3: Update src/lib.rs with new module declarations and re-exports</name>
  <files>src/lib.rs</files>
  <action>
Replace the entire content of src/lib.rs with the updated version:

```rust
#![warn(missing_docs)]

//! herdingcats — a deterministic, undoable game-event engine.

mod action;
mod behavior;
mod engine;
mod hash;
mod mutation;

pub use action::Action;
pub use behavior::Behavior;
pub use engine::Engine;
pub use mutation::Mutation;
```

Key points:
- `mod operation`, `mod rule`, `mod transaction` are REMOVED (replaced by mutation, behavior, action).
- The old files (operation.rs, rule.rs, transaction.rs) still exist on disk at this point but are no longer declared as modules — they will be deleted in Plan 02.
- `RuleLifetime` is NOT re-exported (it's gone).
- `Transaction`, `Operation`, `Rule` are NOT re-exported.
- `engine` module is still declared — engine.rs will be updated in Plan 02 to compile against the new types. At this stage, lib.rs declaring `mod engine` will cause a compile error until Plan 02 updates engine.rs. This is expected — the phase is not fully buildable until Plan 02 completes.

Note to executor: After writing lib.rs, run `cargo check 2>&1 | head -40` to confirm the expected errors are ONLY in engine.rs (referencing old types), and NOT in mutation.rs, behavior.rs, or action.rs themselves.
  </action>
  <verify>
    <automated>grep -c "pub use mutation::Mutation\|pub use behavior::Behavior\|pub use action::Action\|pub use engine::Engine" /Users/mprelee/herdingcats/src/lib.rs</automated>
  </verify>
  <done>src/lib.rs declares modules action, behavior, engine, hash, mutation. Re-exports Mutation, Behavior, Action, Engine. No re-exports of Operation, Rule, Transaction, RuleLifetime.</done>
</task>

</tasks>

<verification>
After completing all three tasks in this plan:

1. `grep -rn "pub trait Mutation" src/mutation.rs` — finds the trait
2. `grep -rn "pub trait Behavior" src/behavior.rs` — finds the trait
3. `grep -rn "pub struct Action" src/action.rs` — finds the struct
4. `grep -n "irreversible\|RuleLifetime" src/action.rs` — returns nothing
5. `cargo check 2>&1 | grep "^error"` — errors exist only in engine.rs (expected), NOT in mutation.rs, behavior.rs, or action.rs

The old files (operation.rs, rule.rs, transaction.rs) still exist but are no longer declared in lib.rs.
</verification>

<success_criteria>
- src/mutation.rs: `pub trait Mutation<S>: Clone` with apply, undo, hash_bytes
- src/behavior.rs: `pub trait Behavior<S, M, I, P>` with before, after, id, priority; uses Action&lt;M&gt; in signatures
- src/action.rs: `pub struct Action<M>` with mutations/deterministic/cancelled fields; no irreversible; no RuleLifetime
- src/lib.rs: declares mod action, behavior, engine, hash, mutation; re-exports Mutation, Behavior, Action, Engine
- Zero occurrences of Operation/Rule/Transaction/RuleLifetime in new files
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-rename/04-01-SUMMARY.md`
</output>
