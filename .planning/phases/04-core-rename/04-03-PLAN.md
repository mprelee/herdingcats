---
phase: 04-core-rename
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - examples/tictactoe.rs
  - examples/backgammon.rs
  - Cargo.toml
autonomous: false
requirements:
  - REN-04

must_haves:
  truths:
    - "examples/tictactoe.rs compiles and runs with identical output under new API names"
    - "examples/backgammon.rs compiles and runs with identical output under new API names"
    - "No reference to Operation, Rule, Transaction, or RuleLifetime remains in either example"
    - "cargo build succeeds with zero warnings"
    - "Cargo.toml version bumped to 0.3.0"
  artifacts:
    - path: "examples/tictactoe.rs"
      provides: "Updated tic-tac-toe example"
      contains: "impl Mutation<Game> for Op"
    - path: "examples/backgammon.rs"
      provides: "Updated backgammon example"
      contains: "impl Mutation<BgState> for BackgammonOp"
    - path: "Cargo.toml"
      provides: "Version bump"
      contains: "version = \"0.3.0\""
  key_links:
    - from: "examples/tictactoe.rs"
      to: "src/lib.rs"
      via: "use herdingcats::*"
      pattern: "use herdingcats"
    - from: "examples/backgammon.rs"
      to: "src/lib.rs"
      via: "use herdingcats::*"
      pattern: "use herdingcats"
---

<objective>
Update both examples to use the new API names (Mutation, Behavior, Action), remove all references to the old names, and bump Cargo.toml to version 0.3.0.

Purpose: Completes the rename — all code in the repository uses the new names. The examples serve as integration verification that the renamed API works end-to-end without behavioral changes.

Output: examples/tictactoe.rs and examples/backgammon.rs compile and run identically to before. Cargo.toml at 0.3.0.
</objective>

<execution_context>
@/Users/mprelee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-core-rename/04-01-SUMMARY.md
@.planning/phases/04-core-rename/04-02-SUMMARY.md
</context>

<interfaces>
<!-- New public API from Plans 01 and 02 that examples must use. -->

Public API (from src/lib.rs after Plans 01+02):
```rust
pub use action::Action;      // was Transaction
pub use behavior::Behavior;  // was Rule
pub use engine::Engine;
pub use mutation::Mutation;  // was Operation
// RuleLifetime: REMOVED
```

Action<M> (was Transaction<O>):
```rust
pub struct Action<M> {
    pub mutations: Vec<M>,    // was: ops: Vec<O>
    pub deterministic: bool,
    pub cancelled: bool,
    // irreversible: REMOVED
}
impl<M> Action<M> { pub fn new() -> Self; }
```

Engine method change:
- `engine.add_behavior(b)` — no lifetime arg (was: engine.add_rule(b, RuleLifetime::Permanent))

Behavior<S,M,I,P> hooks receive Action<M>:
- `fn before(&self, state: &S, event: &mut I, tx: &mut Action<M>)`
- `fn after(&self, state: &S, event: &I, tx: &mut Action<M>)`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update examples/tictactoe.rs</name>
  <files>examples/tictactoe.rs</files>
  <action>
Update examples/tictactoe.rs in place with these exact changes:

1. `impl Operation<Game> for Op` → `impl Mutation<Game> for Op`
   (Method bodies for `apply`, `undo`, `hash_bytes` are UNCHANGED)

2. `impl Rule<Game, Op, GameEvent, GamePriority> for PlayRule` → `impl Behavior<Game, Op, GameEvent, GamePriority> for PlayRule`
   - `fn before(&self, state: &Game, event: &mut GameEvent, tx: &mut Transaction<Op>)` → `fn before(&self, state: &Game, event: &mut GameEvent, tx: &mut Action<Op>)`
   - `tx.ops.push(...)` → `tx.mutations.push(...)`
   (Cancellation logic `tx.cancelled = true` is UNCHANGED)

3. `impl Rule<Game, Op, GameEvent, GamePriority> for WinRule` → `impl Behavior<Game, Op, GameEvent, GamePriority> for WinRule`
   - `fn after(&self, state: &Game, _event: &GameEvent, tx: &mut Transaction<Op>)` → `fn after(&self, state: &Game, _event: &GameEvent, tx: &mut Action<Op>)`
   - `tx.ops.push(...)` → `tx.mutations.push(...)`

4. In `fn main()`:
   - `engine.add_rule(PlayRule, RuleLifetime::Permanent)` → `engine.add_behavior(PlayRule)`
   - `engine.add_rule(WinRule, RuleLifetime::Permanent)` → `engine.add_behavior(WinRule)`
   - `let tx = Transaction::new()` → `let tx = Action::new()`

5. The `use herdingcats::*` glob import at the top brings in all public names — no explicit import changes needed since the glob handles it. But verify: with the glob, `Operation`, `Rule`, `Transaction`, `RuleLifetime` are no longer in scope (they're not re-exported), so any remaining reference to them will be a compile error. That's the compile-time guard.

6. Do NOT change: game state types, game logic, display impl, board layout, move sequence in main. The output of `cargo run --example tictactoe` must be identical to before.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo run --example tictactoe 2>&1</automated>
  </verify>
  <done>`cargo run --example tictactoe` exits 0 and prints the tic-tac-toe board with X winning, identical to the pre-rename output. Zero compilation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update examples/backgammon.rs and bump Cargo.toml version</name>
  <files>examples/backgammon.rs, Cargo.toml</files>
  <action>
**examples/backgammon.rs — update in place:**

1. `impl Operation<BgState> for BackgammonOp` → `impl Mutation<BgState> for BackgammonOp`
   (All method bodies for `apply`, `undo`, `hash_bytes` are UNCHANGED)

2. `impl Rule<BgState, BackgammonOp, BackgammonEvent, BackgammonPriority> for RollDiceRule`
   → `impl Behavior<BgState, BackgammonOp, BackgammonEvent, BackgammonPriority> for RollDiceRule`
   - `fn before(&self, _state: &BgState, event: &mut BackgammonEvent, tx: &mut Transaction<BackgammonOp>)`
     → `fn before(&self, _state: &BgState, event: &mut BackgammonEvent, tx: &mut Action<BackgammonOp>)`
   - `tx.ops.push(...)` → `tx.mutations.push(...)`
   - `tx.irreversible = false` — REMOVE this line. The `irreversible` field no longer exists on `Action`. Replace the "RollDice is non-undoable" comment with: `// Note: Phase 5 will add is_reversible() = false to RollDiceOp to make dice rolls non-undoable.`

3. `impl Rule<BgState, BackgammonOp, BackgammonEvent, BackgammonPriority> for MoveRule`
   → `impl Behavior<BgState, BackgammonOp, BackgammonEvent, BackgammonPriority> for MoveRule`
   - `fn before(&self, state: &BgState, event: &mut BackgammonEvent, tx: &mut Transaction<BackgammonOp>)`
     → `fn before(&self, state: &BgState, event: &mut BackgammonEvent, tx: &mut Action<BackgammonOp>)`
   - `tx.ops.push(op)` → `tx.mutations.push(op)`
   - Update comment: remove mention of "CommitFrame" and "irreversible = true" (the field is gone). Simplify to: "Each Move dispatches via Action::new() — reversible by default in Phase 4."

4. In `fn main()`:
   - `engine.add_rule(RollDiceRule, RuleLifetime::Permanent)` → `engine.add_behavior(RollDiceRule)`
   - `engine.add_rule(MoveRule, RuleLifetime::Permanent)` → `engine.add_behavior(MoveRule)`
   - `let mut tx = Transaction::new()` → `let mut tx = Action::new()`
   - `tx.irreversible = false` — REMOVE this line (in the "Roll dice" section). No replacement needed.
   - `Transaction::new()` in dispatch calls → `Action::new()`

5. In test module and prop test module (at bottom of file):
   - `use super::*` is already present — glob imports handle the rename.
   - `engine.add_rule(RollDiceRule, RuleLifetime::Permanent)` → `engine.add_behavior(RollDiceRule)`
   - `engine.add_rule(MoveRule, RuleLifetime::Permanent)` → `engine.add_behavior(MoveRule)`
   - `Transaction::new()` → `Action::new()`
   - `tx.irreversible = false` (in prop test's dice roll section) → REMOVE this line
   - Any `tx.ops.push(...)` → `tx.mutations.push(...)`
   - Update comments that mention `irreversible` semantics to note "Phase 5 will restore non-undoable dice rolls via is_reversible()".

6. Behavioral note: In Phase 4, dice rolls are now "undoable" (a CommitFrame is pushed). The `engine.undo()` call in main will now undo Move 2 as before, but an additional `engine.undo()` would undo Move 1, and a third would undo the dice roll. The demo's output is unchanged for the sequence shown (only undo once). Tests in the example that previously verified dice rolls were non-undoable (via the absence of a CommitFrame) need to be updated: if any test asserts that `undo()` on a dice roll is a no-op, update or remove it — dice rolls ARE now undoable in Phase 4.

**Cargo.toml — bump version:**

Change `version = "0.2.1"` to `version = "0.3.0"` in Cargo.toml. No other changes to Cargo.toml.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo run --example backgammon 2>&1 && cargo test 2>&1 | tail -15</automated>
  </verify>
  <done>`cargo run --example backgammon` exits 0 and prints the rolling/moving/undoing sequence. `cargo test` passes all tests. Cargo.toml shows version = "0.3.0".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete Phase 4 rename</name>
  <files>examples/tictactoe.rs, examples/backgammon.rs, src/lib.rs</files>
  <action>
Run the six verification commands listed in how-to-verify and confirm all pass before marking approved.
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo build 2>&1 | grep "^error" | head -5</automated>
  </verify>
  <done>All 6 verification checks pass: cargo build clean, cargo test passes, both examples run, no old names in source, Cargo.toml at 0.3.0.</done>
  <what-built>
Complete Phase 4 Core Rename — all three plans executed:
- Plan 01: New module files (mutation.rs, behavior.rs, action.rs) + lib.rs updated
- Plan 02: engine.rs rewritten with new types, old source files deleted
- Plan 03: Both examples updated, Cargo.toml bumped to 0.3.0
  </what-built>
  <how-to-verify>
Run these commands and confirm each passes:

1. `cargo build` — zero warnings, zero errors
2. `cargo test` — all tests pass (check the summary line)
3. `cargo run --example tictactoe` — prints tic-tac-toe board, X wins on move 5
4. `cargo run --example backgammon` — prints dice roll + 2 moves + undo sequence
5. `grep -rn "Operation\|RuleLifetime\|Transaction\b" src/ examples/` — zero matches (confirms old names are gone)
6. `grep "version" Cargo.toml` — shows version = "0.3.0"
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which check failed and the error output.</resume-signal>
</task>

</tasks>

<verification>
Final phase verification:

1. `cargo build` — zero errors, zero warnings
2. `cargo test` — all tests pass
3. `cargo run --example tictactoe` — runs correctly
4. `cargo run --example backgammon` — runs correctly
5. `grep -rn "RuleLifetime" src/ examples/` — zero matches
6. `grep -rn "\bOperation\b\|\bRule\b\|\bTransaction\b" src/ examples/` — zero matches on public trait/struct usages
7. `cat src/lib.rs` — shows Mutation, Behavior, Action, Engine re-exports only
</verification>

<success_criteria>
- `cargo build` succeeds with zero warnings
- `cargo test` passes — all unit tests and doctests reference new names
- `cargo run --example tictactoe` runs without behavioral changes
- `cargo run --example backgammon` runs without behavioral changes
- `RuleLifetime` appears nowhere in public API or examples
- Cargo.toml at version 0.3.0
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-rename/04-03-SUMMARY.md`
</output>
