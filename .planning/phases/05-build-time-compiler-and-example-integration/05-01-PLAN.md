---
phase: 05-build-time-compiler-and-example-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/herdingcats_codegen/Cargo.toml
  - crates/herdingcats_codegen/src/lib.rs
  - crates/herdingcats_codegen/src/parser.rs
  - crates/herdingcats_codegen/src/ast.rs
  - crates/herdingcats_codegen/src/ir.rs
  - crates/herdingcats_codegen/src/validate.rs
  - crates/herdingcats_codegen/src/bindings.rs
  - crates/herdingcats_codegen/src/diagnostics.rs
  - tests/phase5_codegen.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "A separate build-time compiler front-end exists without adding parser dependencies to the runtime crate surface"
    - "Authored DSL files load through pest and lower into AST then validated IR rather than feeding codegen directly"
    - "Binding/config resolution happens during front-end validation, not implicitly during code generation"
    - "Semantic failures from the Phase 4 contract reject direct mutation, forbidden after() semantics, invalid lifetimes, duplicate ids, and unapproved bindings before any Rust emission"
  artifacts:
    - path: crates/herdingcats_codegen/src/ast.rs
      provides: AST types preserving authored rule structure and diagnostics spans
    - path: crates/herdingcats_codegen/src/ir.rs
      provides: validated IR types consumed by later codegen
    - path: crates/herdingcats_codegen/src/validate.rs
      provides: AST to IR lowering plus semantic validation failures
    - path: crates/herdingcats_codegen/src/bindings.rs
      provides: host binding/config loading and resolution
  key_links:
    - from: crates/herdingcats_codegen/src/parser.rs
      to: docs/dsl/grammar-scope.pest
      via: parser front-end honors locked Phase 4 grammar contract
      pattern: "grammar"
    - from: crates/herdingcats_codegen/src/validate.rs
      to: docs/dsl/validation.md
      via: semantic failures mirror the locked validation contract
      pattern: "after|reversible|duplicate"
---

<objective>
Build the Phase 5 compiler front-end: file loading, `pest` grammar wiring, AST types, binding/config loading, parse diagnostics, and AST-to-validated-IR lowering with semantic validation.

Purpose: This plan establishes the only valid input to code generation. By the end, authored DSL files must become validated IR or fail with actionable errors before any Rust emission is attempted.
Output: `herdingcats_codegen` front-end modules plus front-end smoke tests.
</objective>

<execution_context>
@/Users/mprelee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-build-time-compiler-and-example-integration/05-RESEARCH.md
@.planning/phases/04-dsl-scope-and-semantic-contract/04-VERIFICATION.md
@docs/dsl/README.md
@docs/dsl/grammar-scope.pest
@docs/dsl/ir.md
@docs/dsl/validation.md
@docs/dsl/bindings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the codegen crate and parser-to-IR front-end</name>
  <files>Cargo.toml, crates/herdingcats_codegen/Cargo.toml, crates/herdingcats_codegen/src/lib.rs, crates/herdingcats_codegen/src/parser.rs, crates/herdingcats_codegen/src/ast.rs, crates/herdingcats_codegen/src/ir.rs</files>
  <action>
Create a separate build-time companion crate for the DSL compiler front-end.

Required outcomes:
- workspace or path setup that adds `crates/herdingcats_codegen` without changing the runtime crate API
- parser module wired to the Phase 4 grammar contract
- AST types that preserve authored structure and diagnostics spans
- IR types representing only the validated semantic surface expected by later codegen
- public entrypoint in `lib.rs` that exposes parse/load/lower APIs, but does not yet emit Rust

Scope guard:
- no runtime parser dependency in the main `herdingcats` crate
- no Rust emission in this task
- no consumer fixture yet
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test</automated>
  </verify>
  <done>The new codegen crate exists, compiles, and exposes a parser-to-AST/IR front-end surface while the main crate remains runtime-focused.</done>
</task>

<task type="auto">
  <name>Task 2: Add binding/config loading and semantic validation</name>
  <files>crates/herdingcats_codegen/src/validate.rs, crates/herdingcats_codegen/src/bindings.rs, crates/herdingcats_codegen/src/diagnostics.rs, tests/phase5_codegen.rs</files>
  <action>
Implement the semantic-validation layer described by Phase 4.

Required outcomes:
- load consumer binding/config data for approved state/event/op paths
- lower AST to validated IR only when bindings and semantic rules pass
- reject duplicate ids, invalid lifetimes, forbidden `after()` semantics, direct mutation, unsupported transaction flags, and unapproved bindings before codegen
- add focused tests proving front-end success and front-end failures

Design bar:
- diagnostics should identify which authored rule failed and why
- validation behavior must follow the Phase 4 contract rather than inventing looser rules
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test</automated>
  </verify>
  <done>Front-end tests cover both happy-path lowering to validated IR and semantic failure cases defined by Phase 4.</done>
</task>

</tasks>

<verification>
Final gate after both tasks:

```bash
cargo test
test -f crates/herdingcats_codegen/src/parser.rs
test -f crates/herdingcats_codegen/src/ast.rs
test -f crates/herdingcats_codegen/src/ir.rs
test -f crates/herdingcats_codegen/src/validate.rs
test -f crates/herdingcats_codegen/src/bindings.rs
```

Confirm:
- parser/codegen support lives in a separate companion crate
- authored input reaches validated IR without any Rust emission path yet
- semantic failures happen before code generation
- no runtime API redesign was introduced
</verification>

<success_criteria>
1. A companion front-end crate exists and compiles inside the workspace
2. AST and validated IR types are implemented
3. Binding/config loading and semantic validation reject out-of-contract inputs
4. Tests prove front-end success and failure cases without requiring full codegen yet
</success_criteria>

<notes>
This plan is foundational for Phase 5 but does not, by itself, satisfy the phase-level emitted-Rust or consumer build integration requirements. Those are completed in Plans 05-02 and 05-03.
</notes>

<output>
After completion, create `.planning/phases/05-build-time-compiler-and-example-integration/05-01-SUMMARY.md`
</output>
