---
phase: 05-build-time-compiler-and-example-integration
plan: 03
type: execute
wave: 3
depends_on: [05-02-PLAN.md]
files_modified:
  - examples/dsl_consumer/Cargo.toml
  - examples/dsl_consumer/build.rs
  - examples/dsl_consumer/src/main.rs
  - examples/dsl_consumer/rules/example.cats
  - tests/phase5_codegen.rs
autonomous: true
requirements: [INT-01, INT-02]

must_haves:
  truths:
    - "A real consumer target invokes the compiler from build.rs and writes generated Rust into OUT_DIR"
    - "The consumer includes the generated module and registers generated rules alongside at least one handwritten rule into an engine"
    - "At least one authored DSL rule drives a real engine dispatch in the consumer path"
    - "The end-to-end path proves authored DSL -> validated compiler pipeline -> generated Rust -> engine integration"
  artifacts:
    - path: examples/dsl_consumer/build.rs
      provides: build-time invocation of the codegen crate and OUT_DIR writing
    - path: examples/dsl_consumer/src/main.rs
      provides: example consumer using generated registration and real engine dispatch
    - path: examples/dsl_consumer/rules/example.cats
      provides: authored DSL input consumed by the build pipeline
  key_links:
    - from: examples/dsl_consumer/build.rs
      to: crates/herdingcats_codegen/src/lib.rs
      via: build script invokes compiler entrypoint
      pattern: "compile|generate"
    - from: examples/dsl_consumer/src/main.rs
      to: generated module
      via: include/generated registration into Engine
      pattern: "include!|register"
---

<objective>
Prove the end-to-end consumer integration path for the build-time DSL compiler.

Purpose: This plan satisfies the milestone’s integration requirements by building a real consumer example that uses `build.rs`, generated Rust from `OUT_DIR`, generated-rule registration, and a working engine dispatch path.
Output: consumer fixture/example plus end-to-end integration tests.
</objective>

<execution_context>
@/Users/mprelee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-build-time-compiler-and-example-integration/05-RESEARCH.md
@.planning/phases/05-build-time-compiler-and-example-integration/05-02-PLAN.md
@docs/dsl/examples/accepted-rules.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the example consumer fixture with build.rs integration</name>
  <files>examples/dsl_consumer/Cargo.toml, examples/dsl_consumer/build.rs, examples/dsl_consumer/rules/example.cats</files>
  <action>
Create a real consumer target that uses the codegen crate from `build.rs`.

Required outcomes:
- example consumer package manifest
- authored DSL rule file under a dedicated rules directory
- build script that invokes the compiler and writes generated Rust into `OUT_DIR`
- explicit rerun-if-changed behavior for authored rule files and relevant config
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test</automated>
  </verify>
  <done>The example consumer builds through Cargo and its build script runs the DSL compiler successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generated code into an engine and prove one real dispatch flow</name>
  <files>examples/dsl_consumer/src/main.rs, tests/phase5_codegen.rs</files>
  <action>
Complete the example consumer so it uses the generated output at runtime.

Required outcomes:
- include the generated module from `OUT_DIR`
- define or use an operation wrapper path when needed
- register generated rules alongside at least one handwritten rule in the same engine setup
- dispatch at least one event showing the authored DSL rule changes engine behavior
- add test coverage that proves the end-to-end path works
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test</automated>
  </verify>
  <done>The repository contains a real end-to-end consumer example proving authored DSL -> generated Rust -> engine dispatch.</done>
</task>

</tasks>

<verification>
Final gate after both tasks:

```bash
cargo test
test -f examples/dsl_consumer/build.rs
test -f examples/dsl_consumer/src/main.rs
test -f examples/dsl_consumer/rules/example.cats
```

Confirm:
- build.rs drives the compiler
- generated code is included from OUT_DIR
- one real engine dispatch path is demonstrated through the consumer
- generated and handwritten rules coexist in the same consumer integration path
- Phase 5 integration requirements are satisfied without runtime parser behavior
</verification>

<success_criteria>
1. The repository contains a real consumer fixture or example using build.rs
2. Generated Rust is included from OUT_DIR and registered into an engine alongside at least one handwritten rule
3. One authored DSL rule produces a working engine dispatch flow in that mixed setup
4. The end-to-end example satisfies INT-01 and INT-02
</success_criteria>

<output>
After completion, create `.planning/phases/05-build-time-compiler-and-example-integration/05-03-SUMMARY.md`
</output>
