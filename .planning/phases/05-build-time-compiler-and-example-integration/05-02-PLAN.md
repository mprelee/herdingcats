---
phase: 05-build-time-compiler-and-example-integration
plan: 02
type: execute
wave: 2
depends_on: [05-01-PLAN.md]
files_modified:
  - crates/herdingcats_codegen/src/codegen.rs
  - crates/herdingcats_codegen/src/render.rs
  - crates/herdingcats_codegen/src/lib.rs
  - tests/phase5_codegen.rs
autonomous: true
requirements: [GEN-02, GEN-03, GEN-05]

must_haves:
  truths:
    - "Code generation consumes validated IR only"
    - "Generated Rust targets existing Rule, Operation, and Transaction semantics without direct state mutation"
    - "Generated output includes a registration path for generated rules"
    - "Emission order and identifiers are canonical enough to support deterministic generated output"
  artifacts:
    - path: crates/herdingcats_codegen/src/codegen.rs
      provides: IR-to-Rust generation for rules, operations, and registration helpers
    - path: crates/herdingcats_codegen/src/render.rs
      provides: source rendering/writing to OUT_DIR or equivalent writer target
  key_links:
    - from: crates/herdingcats_codegen/src/codegen.rs
      to: src/rule.rs
      via: generated rules implement the existing Rule contract
      pattern: "before"
    - from: crates/herdingcats_codegen/src/codegen.rs
      to: src/operation.rs
      via: emitted mutations become reversible operations
      pattern: "undo|hash_bytes"
---

<objective>
Build the deterministic Rust generation layer for the validated DSL IR.

Purpose: This plan turns validated IR into compileable Rust types and helpers that fit the current engine model. The output must be additive, deterministic, and include a consumer-friendly registration path.
Output: codegen/render modules plus generated-source smoke tests.
</objective>

<execution_context>
@/Users/mprelee/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mprelee/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-build-time-compiler-and-example-integration/05-RESEARCH.md
@.planning/phases/05-build-time-compiler-and-example-integration/05-01-PLAN.md
@docs/dsl/semantics.md
@docs/dsl/ir.md
@src/rule.rs
@src/operation.rs
@src/transaction.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate rules, operations, and registration helpers from validated IR</name>
  <files>crates/herdingcats_codegen/src/codegen.rs, crates/herdingcats_codegen/src/lib.rs</files>
  <action>
Implement the core code generator.

Required outcomes:
- consume validated IR only
- generate reversible operation types or wrappers compatible with `Operation<S>`
- generate rule types implementing `Rule<S, O, E, P>` with `before()` logic only
- generate a registration helper for adding generated rules alongside handwritten ones

Codegen constraints:
- no direct state mutation in generated rule bodies
- no generated `after()` mutation path
- generated rule ids and effect ordering must be canonical
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test</automated>
  </verify>
  <done>The generator emits compileable Rust constructs for operations, rules, and registration helpers from validated IR.</done>
</task>

<task type="auto">
  <name>Task 2: Add rendering/output writing and generated-source smoke tests</name>
  <files>crates/herdingcats_codegen/src/render.rs, tests/phase5_codegen.rs</files>
  <action>
Implement the rendering/output path for generated Rust and add tests that confirm emitted source is stable enough to compile in a consumer context.

Required outcomes:
- render generated Rust text in canonical order
- support writing output to `OUT_DIR` or an equivalent explicit destination
- add smoke tests that compile or otherwise validate generated source shape
- ensure registration helpers and generated operations/rules appear in emitted output
  </action>
  <verify>
    <automated>cd /Users/mprelee/herdingcats && cargo test</automated>
  </verify>
  <done>Generated-source tests prove the codegen layer emits the core Rust artifacts required by consumer integration.</done>
</task>

</tasks>

<verification>
Final gate after both tasks:

```bash
cargo test
test -f crates/herdingcats_codegen/src/codegen.rs
test -f crates/herdingcats_codegen/src/render.rs
```

Confirm:
- generated code comes only from validated IR
- generated operations/rules/registration helpers exist
- emitted output is deterministic enough for fixture compilation
</verification>

<success_criteria>
1. Code generation from validated IR is implemented
2. Generated Rust targets the current engine model additively
3. Registration helpers exist for generated-rule integration
4. Rendered source is validated through smoke tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-build-time-compiler-and-example-integration/05-02-SUMMARY.md`
</output>
